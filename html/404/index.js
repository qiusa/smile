define(['regularjs', '../../javascript/base/util', '../../html/404/index.html'], function(Regular, util, index) {    require('../../css/404.css');    return Regular.extend({        template: index,        data:{        	config: {        		live: false        	}        },        init: function() {        	this.data.config.live = util.getQueryString('live');        	this.$update();        	console.info(this.data)        }    });});